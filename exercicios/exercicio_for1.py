# Escreva um programa que declare uma lista de números inteiros e imprima todos os números pares da lista.

lista = [1, 2, 3, 4, 5, 6, 7, 8]

for element in lista:
    if element % 2 == 0:
        print(element)
